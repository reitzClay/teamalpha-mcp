# This folder contains custom tool implementations.
# Each tool is a Python function that agents can call.

def example_tool(input_data: str) -> str:
    """Example tool that processes input data."""
    return f"Processed: {input_data}"
